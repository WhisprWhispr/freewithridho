rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Semua orang bisa melihat gambar, tapi hanya admin (user login) yang bisa mengupload gambar
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
